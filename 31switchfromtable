Program 31 switchfromtable ;

var a,b,c,d,e,f,g,h,i,j,k,l,leitura:integer;
		key:boolean;
Begin
	key:=true;
	a:=50;
	b:=200;
	c:=555;
	d:=5000;
	e:=700;
	f:=999;
	g:=555;
	h:=5000;
	i:=5554;
	j:=5001;
	k:=4999;
	l:=5555;
	writeln('Este programa lê números inteiros de uma lista previamente definida. Se o número 5000 for lido, um switch é ativado e a lista é impressa até que 5000 seja lido novamente. Se o número 5555 for lido, o programa é encerrado.');
	leitura:=0;
	writeln('O programa vai começar.');
	leitura:=a;
	if leitura=5000 then
		begin
		writeln ('O switch está ativado. Ele mudará assim que encontrar o valor 5000 dentro da lista.');
		key:=not(key);
		if key=false then
			writeln('O programa está lendo os valores, mas não os imprimirá na tela até que encontre o valor 5000 dentre a lista para virar o switch.');		
		if key=true then
			begin
			writeln('O programa está lendo os valores e os imprimirá na tela até que encontre o valor 5000 dentre a lista para virar o switch.');	
	 		writeln ('O valor lido foi: ',leitura);
	 		end
		end
	else
  if key then
		writeln ('O valor lido foi: ',leitura);
	if leitura = 5555 then
		writeln ('O programa leu o valor 5555 e será encerrado.')
	else
  leitura:=b;
	if leitura=5000 then
		begin
		writeln ('O switch está ativado. Ele mudará assim que encontrar o valor 5000 dentro da lista.');
		key:=not(key);
		if key=false then
			writeln('O programa está lendo os valores, mas não os imprimirá na tela até que encontre o valor 5000 dentre a lista para virar o switch.');		
		if key=true then
			begin
			writeln('O programa está lendo os valores e os imprimirá na tela até que encontre o valor 5000 dentre a lista para virar o switch.');	
	 		writeln ('O valor lido foi: ',leitura);
	 		end
		end
	else
  if key then
		writeln ('O valor lido foi: ',leitura);
	if leitura = 5555 then
		writeln ('O programa leu o valor 5555 e será encerrado.')
	else  
	leitura:=c;
	if leitura=5000 then
		begin
		writeln ('O switch está ativado. Ele mudará assim que encontrar o valor 5000 dentro da lista.');
		key:=not(key);
		if key=false then
			writeln('O programa está lendo os valores, mas não os imprimirá na tela até que encontre o valor 5000 dentre a lista para virar o switch.');		
		if key=true then
			begin
			writeln('O programa está lendo os valores e os imprimirá na tela até que encontre o valor 5000 dentre a lista para virar o switch.');	
	 		writeln ('O valor lido foi: ',leitura);
	 		end
		end
	else
  if key then
		writeln ('O valor lido foi: ',leitura);
	if leitura = 5555 then
		writeln ('O programa leu o valor 5555 e será encerrado.')
	else
	leitura:=d;
	if leitura=5000 then
		begin
		writeln ('O switch está ativado. Ele mudará assim que encontrar o valor 5000 dentro da lista.');
		key:=not(key);
		if key=false then
			writeln('O programa está lendo os valores, mas não os imprimirá na tela até que encontre o valor 5000 dentre a lista para virar o switch.');		
		if key=true then
			begin
			writeln('O programa está lendo os valores e os imprimirá na tela até que encontre o valor 5000 dentre a lista para virar o switch.');	
	 		writeln ('O valor lido foi: ',leitura);
	 		end
		end
	else
  if key then
		writeln ('O valor lido foi: ',leitura);
	if leitura = 5555 then
		writeln ('O programa leu o valor 5555 e será encerrado.')
	else
	leitura:=e;
	if leitura=5000 then
		begin
		writeln ('O switch está ativado. Ele mudará assim que encontrar o valor 5000 dentro da lista.');
		key:=not(key);
		if key=false then
			writeln('O programa está lendo os valores, mas não os imprimirá na tela até que encontre o valor 5000 dentre a lista para virar o switch.');		
		if key=true then
			begin
			writeln('O programa está lendo os valores e os imprimirá na tela até que encontre o valor 5000 dentre a lista para virar o switch.');	
	 		writeln ('O valor lido foi: ',leitura);
	 		end
		end
	else
  if key then
		writeln ('O valor lido foi: ',leitura);
	if leitura = 5555 then
		writeln ('O programa leu o valor 5555 e será encerrado.')
	else
	leitura:=f;
	if leitura=5000 then
		begin
		writeln ('O switch está ativado. Ele mudará assim que encontrar o valor 5000 dentro da lista.');
		key:=not(key);
		if key=false then
			writeln('O programa está lendo os valores, mas não os imprimirá na tela até que encontre o valor 5000 dentre a lista para virar o switch.');		
		if key=true then
			begin
			writeln('O programa está lendo os valores e os imprimirá na tela até que encontre o valor 5000 dentre a lista para virar o switch.');	
	 		writeln ('O valor lido foi: ',leitura);
	 		end
		end
	else
  if key then
		writeln ('O valor lido foi: ',leitura);
	if leitura = 5555 then
		writeln ('O programa leu o valor 5555 e será encerrado.')
	else
	leitura:=g;
	if leitura=5000 then
		begin
		writeln ('O switch está ativado. Ele mudará assim que encontrar o valor 5000 dentro da lista.');
		key:=not(key);
		if key=false then
			writeln('O programa está lendo os valores, mas não os imprimirá na tela até que encontre o valor 5000 dentre a lista para virar o switch.');		
		if key=true then
			begin
			writeln('O programa está lendo os valores e os imprimirá na tela até que encontre o valor 5000 dentre a lista para virar o switch.');	
	 		writeln ('O valor lido foi: ',leitura);
	 		end
		end
	else
  if key then
		writeln ('O valor lido foi: ',leitura);
	if leitura = 5555 then
		writeln ('O programa leu o valor 5555 e será encerrado.')
	else
	leitura:=h;
	if leitura=5000 then
		begin
		writeln ('O switch está ativado. Ele mudará assim que encontrar o valor 5000 dentro da lista.');
		key:=not(key);
		if key=false then
			writeln('O programa está lendo os valores, mas não os imprimirá na tela até que encontre o valor 5000 dentre a lista para virar o switch.');		
		if key=true then
			begin
			writeln('O programa está lendo os valores e os imprimirá na tela até que encontre o valor 5000 dentre a lista para virar o switch.');	
	 		writeln ('O valor lido foi: ',leitura);
	 		end
		end
	else
  if key then
		writeln ('O valor lido foi: ',leitura);
	if leitura = 5555 then
		writeln ('O programa leu o valor 5555 e será encerrado.')
	else
	leitura:=i;
	if leitura=5000 then
		begin
		writeln ('O switch está ativado. Ele mudará assim que encontrar o valor 5000 dentro da lista.');
		key:=not(key);
		if key=false then
			writeln('O programa está lendo os valores, mas não os imprimirá na tela até que encontre o valor 5000 dentre a lista para virar o switch.');		
		if key=true then
			begin
			writeln('O programa está lendo os valores e os imprimirá na tela até que encontre o valor 5000 dentre a lista para virar o switch.');	
	 		writeln ('O valor lido foi: ',leitura);
	 		end
		end
	else
  if key then
		writeln ('O valor lido foi: ',leitura);
	if leitura = 5555 then
		writeln ('O programa leu o valor 5555 e será encerrado.')
	else
	leitura:=j;
	if leitura=5000 then
		begin
		writeln ('O switch está ativado. Ele mudará assim que encontrar o valor 5000 dentro da lista.');
		key:=not(key);
		if key=false then
			writeln('O programa está lendo os valores, mas não os imprimirá na tela até que encontre o valor 5000 dentre a lista para virar o switch.');		
		if key=true then
			begin
			writeln('O programa está lendo os valores e os imprimirá na tela até que encontre o valor 5000 dentre a lista para virar o switch.');	
	 		writeln ('O valor lido foi: ',leitura);
	 		end
		end
	else
  if key then
		writeln ('O valor lido foi: ',leitura);
	if leitura = 5555 then
		writeln ('O programa leu o valor 5555 e será encerrado.')
	else
	leitura:=k;
	if leitura=5000 then
		begin
		writeln ('O switch está ativado. Ele mudará assim que encontrar o valor 5000 dentro da lista.');
		key:=not(key);
		if key=false then
			writeln('O programa está lendo os valores, mas não os imprimirá na tela até que encontre o valor 5000 dentre a lista para virar o switch.');		
		if key=true then
			begin
			writeln('O programa está lendo os valores e os imprimirá na tela até que encontre o valor 5000 dentre a lista para virar o switch.');	
	 		writeln ('O valor lido foi: ',leitura);
	 		end
		end
	else
  if key then
		writeln ('O valor lido foi: ',leitura);
	if leitura = 5555 then
		writeln ('O programa leu o valor 5555 e será encerrado.')
	else
	leitura:=l;
	if leitura=5000 then
		begin
		writeln ('O switch está ativado. Ele mudará assim que encontrar o valor 5000 dentro da lista.');
		key:=not(key);
		if key=false then
			writeln('O programa está lendo os valores, mas não os imprimirá na tela até que encontre o valor 5000 dentre a lista para virar o switch.');		
		if key=true then
			begin
			writeln('O programa está lendo os valores e os imprimirá na tela até que encontre o valor 5000 dentre a lista para virar o switch.');	
	 		writeln ('O valor lido foi: ',leitura);
	 		end
		end
	else
  if key then
		writeln ('O valor lido foi: ',leitura);
	if leitura = 5555 then
		writeln ('O programa leu o valor 5555 e será encerrado.')
	else
End.
